tasks:
  hello:
    args:
      first: default
      last: task
    summary: Hello task
    commands:
      - echo Hello {{ .first }} {{ .last }}
    usage: "[--first] [--last]"
  default:
    tasks:
      - hello