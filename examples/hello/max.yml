variables:
  NAME: Fredrik

tasks:
  hello:
    args:
      first_a: default
      last: task
    summary: Hello task
    commands:
      - echo Hello {{ .first_a }} {{ .last }}
    usage: "[--first-a] [--last]"
  default:
    tasks:
      - hello
  hej:
    commands:
      - echo Hej $NAME
  hellocron:
    interval: "* * * * * *"
    commands:
      - echo Hello, world!