tasks:
  hello:
    args:
      first_a: default
      last: task
    summary: Hello task
    commands:
      - echo Hello {{ .first_a }} {{ .last }}
    usage: "[--first] [--last]"
  default:
    tasks:
      - hello